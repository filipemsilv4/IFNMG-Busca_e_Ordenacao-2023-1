#ifndef __sort_hpp__
#define __sort_hpp__

#include <vector>
#include <string>

using namespace std;

void sort_wrapper(vector<int>& v, string algorithm, int left, int right, bool show_list_state);

#endif